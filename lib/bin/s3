#!/bin/bash
if [ -d "/Volumes/PHOTOS" ]; then
  # Control will enter here if $DIRECTORY exists.
  sudo aws s3 sync /Volumes/PHOTOS s3://drakephoto/
else
  echo "dock:  /Volumes/PHOTOS not found, skipping backup."
fi
